name: Build and Deploy LinguaForge APK

on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v3

      - name: ☕ Set up Java
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: 🛠️ Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.22.0'

      # ❌ Temporär deaktiviert: Erstellt neues Flutter-Projekt und überschreibt bestehende Struktur
      # - name: 🧱 Create new Flutter project
      #   run: flutter create -t app lingua_forge_clean

      # ❌ Kritisch: Löscht dein gesamtes Projekt und ersetzt es durch lingua_forge_clean
      # - name: 🔁 Replace old project with new (Root-Level)
      #   run: |
      #     # ⚠️ Deaktiviert, da dieser Schritt dein Projekt bei jedem Build gelöscht hat
      #     # rm -rf lib assets pubspec.yaml android ios macos windows linux web test analysis_options.yaml .metadata README.md
      #     # cp -r lingua_forge_clean/* .
      #     # rm -rf lingua_forge_clean

      # ❌ Automatischer Commit der Clean-Version — deaktiviert, um dein Repo zu schützen
      # - name: 🧠 Commit new project before pulling
      #   run: |
      #     # git config --global user.email "ci@linguaforge.com"
      #     # git config --global user.name "LinguaForge CI"
      #     # git remote set-url origin https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/lieberschwager/lingua_forge.git
      #     # git add -A
      #     # git diff --cached --quiet || git commit -m "CI: Übernehme lingua_forge_clean direkt ins Repository"

      # ❌ Pull & Push nach automatischem Commit — ebenfalls deaktiviert
      # - name: 🔄 Pull latest changes and push
      #   run: |
      #     # git pull origin main --rebase
      #     # git push origin main

      - name: 🚀 Build APK
        run: |
          flutter pub get
          flutter build apk --release

      - name: 📁 Prepare upload folder
        run: mkdir -p upload

      - name: 📤 Move APK to upload folder
        run: |
          cp build/app/outputs/flutter-apk/app-release.apk upload/

      - name: 📎 Upload APK as artifact
        uses: actions/upload-artifact@v4
        with:
          name: lingua_forge-release-apk
          path: upload/app-release.apk