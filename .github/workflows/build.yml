name: Build APK

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Java 17
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.22.0'

      - name: Create fresh Flutter project
        run: flutter create -t app lingua_forge_clean

      - name: Remove alte Projektstruktur
        run: |
          rm -rf lingua_forge/android
          rm -rf lingua_forge/lib
          rm -rf lingua_forge/assets
          rm lingua_forge/pubspec.yaml

      - name: Copy source files
        run: |
          cp -r lingua_forge/lib lingua_forge_clean/
          cp -r lingua_forge/assets lingua_forge_clean/
          cp lingua_forge/pubspec.yaml lingua_forge_clean/

      - name: Rename new project to original name
        run: |
          rm -rf lingua_forge
          mv lingua_forge_clean lingua_forge

      - name: Clean Flutter project
        run: |
          cd lingua_forge
          flutter clean

      - name: Install dependencies
        run: |
          cd lingua_forge
          flutter pub get

      - name: Build APK (release)
        run: |
          cd lingua_forge
          flutter build apk --release

      - name: Ensure upload folder exists
        run: mkdir -p upload

      - name: Move APK if it exists
        run: |
          if [ -f lingua_forge/build/app/outputs/flutter-apk/app-release.apk ]; then
            echo "✅ APK found. Moving to upload folder..."
            cp lingua_forge/build/app/outputs/flutter-apk/app-release.apk upload/
          else
            echo "❌ No APK found. Build may have failed."
          fi

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: lingua_forge-release-apk
          path: upload/app-release.apk